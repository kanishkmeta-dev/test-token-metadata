
# Test Token Metadata + API (Research Only)

This package contains:
- metadata.json (for GitHub Pages hosting)
- API endpoints (for Vercel deployment)
- Example test values

Edit `metadata.json` and replace:
https://your-vercel-app-url
with your real deployed Vercel URL.
